<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/background_rank"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_result"
    tools:context="com.example.blozzum.matchrider.ResultActivity">

    <TextView
        android:id="@+id/timeScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:gravity="center"
        android:text="Your Time\n 0 minute 0 second"
        android:textColor="#000000"
        android:textSize="29dp"/>

    <Button
        android:background="@drawable/buttonshape"
        android:id="@+id/tryAgain"
        android:layout_below="@id/timeScore"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_alignStart="@id/timeScore"
        android:layout_alignEnd="@id/timeScore"
        android:text="Try Again"/>

    <Button
        android:background="@drawable/buttonshape"
        android:id="@+id/mainMenu"
        android:layout_marginTop="10dp"
        android:layout_below="@id/tryAgain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignStart="@id/tryAgain"
        android:layout_alignEnd="@id/tryAgain"
        android:text="Main Menu"/>



</RelativeLayout>
